NATS_URL={{ aura_agent_nats_url }}
# aura_cluster_id is the database UUID injected by the web bootstrap route.
# Falls back to slurm_cluster_name for manual/legacy runs.
CLUSTER_ID={{ aura_cluster_id | default(slurm_cluster_name) }}
ANSIBLE_PLAYBOOK_DIR={{ aura_agent_playbook_dir | default('/opt/aura/ansible') }}
SLURM_USER={{ slurm_user | default('slurm') }}
