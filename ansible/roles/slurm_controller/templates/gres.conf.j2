# Managed by Aura — do not edit manually
{% for node in slurm_nodes %}
{% if node.gpus is defined and node.gpus > 0 %}
NodeName={{ node.expression }} Name=gpu File=/dev/nvidia[0-{{ node.gpus - 1 }}] Count={{ node.gpus }}
{% endif %}
{% endfor %}
